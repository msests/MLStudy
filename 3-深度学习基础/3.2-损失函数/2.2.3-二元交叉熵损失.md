## 定义

**二元交叉熵损失（Binary Cross Entropy Loss, BCE Loss）​** 是用于二分类任务的常用损失函数，衡量模型预测概率与真实标签之间的差异。

对于单个样本的损失计算：
$$
L = -y \log(p) - (1 - y) \log(1 - p)
$$
其中：
- $y$：真实标签（0 或 1）
- $p$：预测概率（取值范围 $[0,1]$）

二元交叉熵损失是多元交叉熵损失的特例，梯度计算和优缺点参考多元交叉熵损失。